# This workflow will run daily at 06:45.
# It will install Python dependencies and run tests with a variety of Python versions.
# See documentation for help debugging smoke test issues:
#    https://lincc-ppt.readthedocs.io/en/latest/practices/ci_testing.html#version-culprit

#rail render-nb docs/notebooks/*.ipynb
#rail render-nb rail/examples/core_examples/*.ipynb docs/notebooks
#rail render-nb rail/examples/core_examples/FluxtoMag_and_Deredden_example.ipynb docs/notebooks
#rail render-nb docs/notebooks/core_examples/FluxtoMag_and_Deredden_example.ipynb docs/notebooks
        
name: Render Notebooks

on:
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10']

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install pandoc
        python -m pip install --upgrade pip
        pip install wheel numpy
        pip install jupyter nbconvert nbsphinx
        git clone https://github.com/LSSTDESC/rail.git
        cd rail
        pip install .[dev]
    - name: List dependencies
      run: |
        pip list
    - name: Run notebooks 
      run: |
        python render_script.py core
        python render_script.py creation
    - uses: stefanzweifel/git-auto-commit-action@v4
      if: always()
